You are a Python performance engineer. Give me ONLY the optimized replacement functions, no explanation needed.

PROBLEM: 21504 combos, each filtering 10.4M rows. Here are the 3 functions to optimize:

FUNCTION 1 - _apply_string_rules (called 21504 times):
def _apply_string_rules(stats_df, rules, sort_field, top_n, sort_dir="desc"):
    filtered = stats_df.copy()  # COPIES 11K DF EVERY TIME
    if "atr_signal" in rules: filtered = filtered[filtered["atr_signal"] == rules["atr_signal"]]
    if "atr_streak_gt" in rules: filtered = filtered[filtered["atr_streak"].fillna(0) > rules["atr_streak_gt"]]
    if "accel_crossed_up" in rules: filtered = filtered[filtered["accel_crossed_up"] == rules["accel_crossed_up"]]
    if "accel_signal" in rules: filtered = filtered[filtered["accel_signal"] == rules["accel_signal"]]
    if "min_wa" in rules: filtered = filtered[filtered["weighted_alpha"].fillna(0) >= rules["min_wa"]]
    if "min_prob_1d" in rules: filtered = filtered[filtered["prob_up_1d"].fillna(0) >= rules["min_prob_1d"]]
    if "min_streak" in rules: filtered = filtered[filtered["streak"].fillna(0) >= rules["min_streak"]]
    if "min_confluence" in rules: filtered = filtered[filtered["confluence"].fillna(0) >= rules["min_confluence"]]
    if sort_field in filtered.columns:
        filtered = filtered.sort_values(sort_field, ascending=(sort_dir == "asc"), na_position="last")
    return filtered.head(top_n).to_dict("records")

FUNCTION 2 - _backtest_string (called 21504 times):
def _backtest_string(symbols_data, hs_cache, st_period=14, st_mult=1.0):
    metrics = {"total_entries": 0, "win_rate": 0, "avg_1d_return": 0, "median_1d_return": 0,
        "std_1d_return": 0, "max_1d_gain": 0, "max_1d_loss": 0,
        "max_consecutive_wins": 0, "max_consecutive_losses": 0, "sharpe_1d": 0,
        "prob_up_1d": 0, "prob_up_1pct": 0, "prob_up_2pct": 0, "prob_down_2pct": 0,
        "prob_up_after_st_cross": 50, "prob_up_after_wa_high": 50}
    if not symbols_data or hs_cache.empty: return metrics
    syms = [s["symbol"] for s in symbols_data]
    basket_hs = hs_cache[hs_cache["symbol"].isin(syms)].copy()  # FILTERS 10.4M ROWS
    if basket_hs.empty: return metrics
    daily_returns = []
    dates = sorted(basket_hs["date"].unique())
    for date in dates:  # PYTHON LOOP OVER EVERY DATE
        day_data = basket_hs[basket_hs["date"] == date]
        if len(day_data) < len(syms) * 0.5: continue
        avg_return = day_data["next_day_return"].mean()
        if not np.isnan(avg_return): daily_returns.append(avg_return)
    if not daily_returns: return metrics
    returns = np.array(daily_returns)
    metrics["total_entries"] = len(returns)
    metrics["win_rate"] = round(float((returns > 0).sum() / len(returns) * 100), 2)
    metrics["avg_1d_return"] = round(float(returns.mean()), 4)
    metrics["median_1d_return"] = round(float(np.median(returns)), 4)
    metrics["std_1d_return"] = round(float(returns.std()), 4)
    metrics["max_1d_gain"] = round(float(returns.max()), 4)
    metrics["max_1d_loss"] = round(float(returns.min()), 4)
    metrics["sharpe_1d"] = round(float(returns.mean() / (returns.std() + 1e-10)), 4)
    metrics["prob_up_1d"] = round(float((returns > 0).sum() / len(returns) * 100), 2)
    metrics["prob_up_1pct"] = round(float((returns > 1).sum() / len(returns) * 100), 2)
    metrics["prob_up_2pct"] = round(float((returns > 2).sum() / len(returns) * 100), 2)
    metrics["prob_down_2pct"] = round(float((returns < -2).sum() / len(returns) * 100), 2)
    max_wins = max_losses = current_wins = current_losses = 0
    for r in returns:
        if r > 0: current_wins += 1; current_losses = 0; max_wins = max(max_wins, current_wins)
        else: current_losses += 1; current_wins = 0; max_losses = max(max_losses, current_losses)
    metrics["max_consecutive_wins"] = max_wins
    metrics["max_consecutive_losses"] = max_losses
    return metrics

CONTEXT: hs_cache has 10.4M rows loaded from historical_screener. stats_df has 11K rows. 21504 combos total. Key bottleneck is filtering hs_cache by symbol list 21504 times.

GIVE ME THE EXACT OPTIMIZED CODE for _apply_string_rules and _backtest_string. Keep same function signatures and return values.
